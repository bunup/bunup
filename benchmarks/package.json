{
    "scripts": {
        "benchmark": "bun run src/main.mjs && node src/tsup.mjs"
    },
    "devDependencies": {
        "bunup": "workspace:*",
        "tsdown": "^0.9.1",
        "tsup": "^8.4.0",
        "unbuild": "^3.5.0"
    },
    "dependencies": {
        "@types/node": "^22.14.1"
    }
}
