{
	"name": "bunup-monorepo",
	"workspaces": [
		"docs",
		"packages/**"
	],
	"scripts": {
		"prepublishOnly": "bun run build",
		"prepare": "bun simple-git-hooks && cd packages/shared && bunx bunup@latest",
		"build": "bun packages/bunup/src/cli/index.ts",
		"build:docs": "bun run --cwd docs build",
		"dev": "bun packages/bunup/src/cli/index.ts --watch",
		"dev:docs": "bun run --cwd docs dev",
		"lint": "oxlint",
		"format": "oxfmt",
		"test": "bun test",
		"test:coverage": "bun test --coverage",
		"test:build": "bun run --cwd packages/bunup/test build",
		"type-check": "bun run --filter './packages/*' type-check",
		"release": "bumpp -r --commit --push --tag",
		"update-deps": "bun update && bun update --filter '*'"
	},
	"devDependencies": {
		"@types/bun": "^1.3.11",
		"@typescript/native-preview": "^7.0.0-dev.20260330.1",
		"bumpp": "^11.0.1",
		"oxfmt": "^0.42.0",
		"oxlint": "^1.57.0",
		"simple-git-hooks": "^2.13.1",
		"typescript": "^6.0.2"
	},
	"simple-git-hooks": {
		"pre-commit": "bun run lint && bun run type-check"
	}
}
